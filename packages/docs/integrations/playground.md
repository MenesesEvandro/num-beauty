# Interactive Playground

Experiment with `num-beauty` options in real-time.

<InteractivePlayground />

## How it works

The playground above uses the `beautify` function directly. You can copy the generated code snippet to use in your project.
